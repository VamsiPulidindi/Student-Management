services:

  mysql:

    image: mysql:8

    environment:
      MYSQL_ROOT_PASSWORD: root123

    volumes:
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql

  flask:

    build: ./flask-app

    ports:
      - "5000:5000"

    depends_on:
      - mysql